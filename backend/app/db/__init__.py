"""数据访问层（SQLAlchemy，同步）。

这一层只做「把表映射成对象」与「提供会话」，**不做任何业务判断** ——
业务规则一律在 `app/services/`。

为什么单独一个包而不是塞进 `app/models/`：
`app/models/` 放的是**对外接口契约**（Pydantic），前端字段与它逐字对应；
ORM 表是**存储实现**，两者的变更节奏与理由完全不同。混在一起会出现
「改了一个不想对外暴露的列，结果前端契约也跟着变」。
"""
