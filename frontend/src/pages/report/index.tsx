/**
 * 冒险日志（主视图将在 Phase 3 实现 —— 对应原型 03 的五屏）。
 *
 * 本页是 5 个 tab 之一，标记为**待替换的占位**：
 * Phase 3 会用「正确率环 + 答对/答错/平均用时三卡 + 百分位条」替换本文件内容。
 */

import ComingSoon from '../../components/ComingSoon'

export default function ReportPage() {
  return (
    <ComingSoon
      navTitle='冒险日志'
      tabKey='report'
      headline='日志本还是空的'
      description='完成一次副本挑战，这里就会出现你的正确率、答对题数与复习建议。'
      sprite='scholar'
    />
  )
}
