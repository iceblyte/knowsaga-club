# 配置文档参考 https://taro-docs.jd.com/docs/next/env-mode-config
# TARO_APP_ID="开发环境下的小程序 AppID"

# 后端地址。
# ⚠️ 必须在这里**真的定义**这个键：Taro 用 webpack DefinePlugin 做文本替换，
# 而它只为「env 文件里出现过的键」生成定义。没定义过的键不会被替换，
# 源码里的 process.env.TARO_APP_API_BASE_URL 会原样留下 —— 小程序里没有
# process 对象，运行时直接报错。见 src/constants/api.ts 的说明。
#
# 微信开发者工具里需要勾选「不校验合法域名」，否则 127.0.0.1 会被拦。
TARO_APP_API_BASE_URL="http://127.0.0.1:8000"
